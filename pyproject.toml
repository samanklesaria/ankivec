[project]
name = "ankivec"
version = "0.1.0"
description = "Anki extension for vector embeddings and hybrid search"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ollama>=0.1.0",
    "chromadb>=0.4.0",
]
[project.optional-dependencies]
testing = [
    "pytest>=8.4.2",
]
