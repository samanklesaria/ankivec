[project]
name = "ankivec"
version = "0.1.0"
description = "Anki extension for vector embeddings and hybrid search"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "chromadb>=0.4.0",
    "ollama>=0.1.0",
]
